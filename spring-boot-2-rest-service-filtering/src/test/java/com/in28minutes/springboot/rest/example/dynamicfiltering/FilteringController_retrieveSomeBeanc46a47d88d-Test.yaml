# Test generated by RoostGPT for test dm-example2 using AI Model gpt

config:
  target: "http://localhost:8080"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/filtering"
          expect:
            - statusCode: 200
            - hasProperty: "field1"
            - hasProperty: "field2"
            - equals: 
                - "field3": null

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/filtering"
          headers:
            invalidHeader: "invalidValue"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - get:
          url: "/filtering?param=@@@@@#####"
          expect:
            - statusCode: 200
            - hasProperty: "field1"
            - hasProperty: "field2"
            - equals: 
                - "field3": null
      - get:
          url: "/filtering?param=veryLargeData"
          expect:
            - statusCode: 200
            - hasProperty: "field1"
            - hasProperty: "field2"
            - equals: 
                - "field3": null