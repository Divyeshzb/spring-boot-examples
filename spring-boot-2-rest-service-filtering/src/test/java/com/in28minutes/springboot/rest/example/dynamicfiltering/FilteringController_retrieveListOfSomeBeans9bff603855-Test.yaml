# Test generated by RoostGPT for test dm-example2 using AI Model gpt

config:
  target: "http://localhost:8080"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/filtering-list"
          expect:
            - statusCode: 200
            - hasProperty: "value1"
            - hasProperty: "value2"
            - hasProperty: "value3"
  - name: "API request with validation failure"
    flow:
      - get:
          url: "/filtering-list"
          headers:
            invalidHeader: "invalidValue"
          expect:
            - statusCode: 400
            - hasProperty: "error"
  - name: "API request with edge case"
    flow:
      - get:
          url: "/filtering-list"
          headers:
            largeDataHeader: "largeDataValue"
          expect:
            - statusCode: 200
            - hasProperty: "value1"
            - hasProperty: "value2"
            - hasProperty: "value3"